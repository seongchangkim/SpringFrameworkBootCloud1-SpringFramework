<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloud.porforio.UserMapper">
	<insert id="insert">
		insert into user(id, password, name, email, phonenumber, regdate) 
			values(#{id},#{password},#{name},#{email},#{phonenumber},now())
	</insert>
</mapper>